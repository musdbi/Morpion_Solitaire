package test;
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;
import java.awt.Point;
import components.PlayedPoint;

public class PointTest {
	
	@Test
	public void testSetStatus() {
//		assertEquals(0, Point.getPlayedPointsCount());
//
//		Point point1 = new Point(0, 0, Status.NORMAL);
//		
//		assertEquals(0, point1.getId());
//		assertEquals(0, Point.getPlayedPointsCount());
//		
//		point1.setStatus(Status.PLAYED);
//		assertEquals(1, point1.getId());
//		assertEquals(1, Point.getPlayedPointsCount());
//		
//		Point point2 = new Point(0, 0, Status.PLAYED);
//		assertEquals(2, point2.getId());
//		assertEquals(2, Point.getPlayedPointsCount());
//		
	}
}
